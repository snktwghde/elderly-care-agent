---
phase: 10-digital-health-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/add_health_card_columns.sql
  - src/webhook/whatsapp.js
autonomous: false
requirements:
  - REQ-10-01
  - REQ-10-08

must_haves:
  truths:
    - "care_recipients table in Supabase has five new nullable columns: blood_group, allergies, major_illnesses, surgeries, medical_history"
    - "Trial started message says ₹499/month (not ₹299)"
    - "Expired subscription messages say ₹499/month in all three languages"
    - "RAZORPAY_PLAN_ID in Railway env vars points to the new ₹499 plan"
  artifacts:
    - path: "supabase/add_health_card_columns.sql"
      provides: "ALTER TABLE migration for health card columns"
      contains: "ALTER TABLE care_recipients"
    - path: "src/webhook/whatsapp.js"
      provides: "Updated price strings"
      contains: "₹499"
  key_links:
    - from: "supabase/add_health_card_columns.sql"
      to: "care_recipients in Supabase"
      via: "manual SQL Editor run"
      pattern: "ADD COLUMN IF NOT EXISTS blood_group"
    - from: "src/services/razorpay.js createSubscription()"
      to: "RAZORPAY_PLAN_ID env var"
      via: "config.razorpay.planId"
      pattern: "config\\.razorpay\\.planId"
---

<objective>
Add the five health card columns to care_recipients in Supabase and update the pricing strings from ₹299 to ₹499.

Purpose: This is the foundation for Phase 10 — the new columns must exist before any health card data can be saved. The price update reflects the new single ₹499/month plan.
Output: SQL migration file + updated price display strings in whatsapp.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-digital-health-card/10-CONTEXT.md
@src/webhook/whatsapp.js
@supabase/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create health card columns migration SQL</name>
  <files>supabase/add_health_card_columns.sql</files>
  <read_first>
    - supabase/schema.sql (lines 30-50 — care_recipients table definition, understand existing column types)
    - supabase/add_rls_policies.sql (understand existing RLS — "service role only" policy already covers new columns)
  </read_first>
  <action>
Create a new file `supabase/add_health_card_columns.sql`. This file will be run manually in the Supabase SQL Editor. Do NOT run it via code.

Write the following SQL exactly (column names and types are locked per CONTEXT.md decisions):

```sql
-- CareProxy — Phase 10: Digital Health Card columns
-- Run this in Supabase SQL Editor: supabase.com → your project → SQL Editor
-- Existing RLS policy "service role only" on care_recipients covers these columns automatically.
-- No new RLS policy needed.

ALTER TABLE care_recipients
  ADD COLUMN IF NOT EXISTS blood_group    text,
  ADD COLUMN IF NOT EXISTS allergies      jsonb DEFAULT '[]'::jsonb,
  ADD COLUMN IF NOT EXISTS major_illnesses text[] DEFAULT '{}',
  ADD COLUMN IF NOT EXISTS surgeries      jsonb DEFAULT '[]'::jsonb,
  ADD COLUMN IF NOT EXISTS medical_history text;
```

Storage formats (per CONTEXT.md, locked):
- blood_group: plain text, nullable (e.g., "B+", "O-")
- allergies: jsonb array of {type: "medicine"|"food"|"other", name: "string"}
- major_illnesses: text[] (postgres array of strings)
- surgeries: jsonb array of {name: "string", date: "string"}
- medical_history: plain text, nullable

All columns are nullable — partial health card setup is allowed.

ponytail: No new table, no new index, no triggers needed. Columns on existing table, existing RLS covers them.
  </action>
  <verify>
    <automated>grep -n "ADD COLUMN IF NOT EXISTS blood_group" supabase/add_health_card_columns.sql</automated>
  </verify>
  <done>
    File exists. Contains ALTER TABLE care_recipients with all five ADD COLUMN IF NOT EXISTS lines. Blood_group is text, allergies is jsonb with default '[]', major_illnesses is text[] with default '{}', surgeries is jsonb with default '[]', medical_history is text.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ₹299 → ₹499 price strings in whatsapp.js</name>
  <files>src/webhook/whatsapp.js</files>
  <read_first>
    - src/webhook/whatsapp.js (lines 1006-1040 — sendTrialStartedMessage and getExpiredReply functions)
  </read_first>
  <action>
Update exactly three price string occurrences in `src/webhook/whatsapp.js`. No other changes.

In `sendTrialStartedMessage` (around line 1012), find:
```
subscribe for ₹299/month to keep using CareProxy
```
Replace with:
```
subscribe for ₹499/month to keep using CareProxy
```

In `getExpiredReply` (around lines 1035-1037), find and replace three language variants:
- English: `Subscribe for ₹299/month to continue` → `Subscribe for ₹499/month to continue`
- Marathi: `₹299/महिना subscribe करा` → `₹499/महिना subscribe करा`
- Hindi: `₹299/महीना subscribe करके` → `₹499/महीना subscribe करके`

That is four total replacements (one in sendTrialStartedMessage, three in getExpiredReply).

No other code changes in this file. The Razorpay plan ID itself comes from the RAZORPAY_PLAN_ID env var via config.razorpay.planId — no code change needed there.

ponytail: src/services/razorpay.js already reads plan_id from config.razorpay.planId. Changing the Railway env var is all that's needed for the plan switch. The code change here is only the user-facing price display strings.
  </action>
  <verify>
    <automated>grep -c "₹499" src/webhook/whatsapp.js</automated>
  </verify>
  <done>
    grep returns 4 or more (sendTrialStartedMessage has 1, getExpiredReply has 3). Zero occurrences of "₹299" remain in the file.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>SQL migration file created (Task 1) and price strings updated (Task 2). Both are done and ready. This checkpoint handles the two actions that require external dashboard access.</what-built>
  <how-to-verify>
    Step A — Run the SQL migration in Supabase:
    1. Open supabase.com → your project → SQL Editor
    2. Open the file supabase/add_health_card_columns.sql (or copy its contents)
    3. Paste into the SQL Editor and click Run
    4. You should see "Success. No rows returned."
    5. Verify in Table Editor → care_recipients → you see new columns: blood_group, allergies, major_illnesses, surgeries, medical_history

    Step B — Create ₹499 Razorpay plan:
    1. Log in to Razorpay Dashboard → Products → Subscriptions → Plans → Create Plan
    2. Set: Plan Name = "CareProxy Monthly", Billing Cycle = Monthly, Billing Amount = ₹499 (enter 49900 paise if prompted)
    3. Copy the new Plan ID (format: plan_XXXXXXXXXXXXXXXX)

    Step C — Update Railway environment variable:
    1. Go to Railway dashboard → elderly-care-agent project → Variables tab
    2. Find RAZORPAY_PLAN_ID and update its value to the new plan ID from Step B
    3. Railway will auto-redeploy. Wait for the deploy to show "Active".

    Confirm: The new plan in Razorpay Dashboard shows Amount: ₹499/month and Status: Active.
  </how-to-verify>
  <resume-signal>Type "done" when Railway has redeployed and you have confirmed the new Razorpay ₹499 plan is active</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL Editor → Supabase DB | Manual operator runs migration; no automated execution |
| Razorpay Dashboard → Railway env var | Manual operator sets new plan ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information Disclosure | care_recipients new columns (PHI) | mitigate | Existing "service role only" RLS policy on care_recipients covers new columns automatically — no anon key access possible |
| T-10-01-02 | Tampering | Price strings in whatsapp.js | accept | Strings are display-only; actual amount enforced by Razorpay API plan configuration. Incorrect display string is low risk. |
| T-10-01-03 | Denial of Service | Large jsonb payloads in allergies/surgeries columns | accept | Service role key writes only; user input is validated in Plan 02 before insertion |
</threat_model>

<verification>
After completing all three tasks and the human checkpoint:

1. SQL columns confirmed in Supabase Table Editor
2. `grep -c "₹499" src/webhook/whatsapp.js` returns >= 4
3. `grep "₹299" src/webhook/whatsapp.js` returns empty (no matches)
4. Railway environment shows updated RAZORPAY_PLAN_ID
5. Railway deployment status is "Active"
</verification>

<success_criteria>
- care_recipients table has five new nullable columns (blood_group, allergies, major_illnesses, surgeries, medical_history)
- All price display strings in whatsapp.js show ₹499/month
- RAZORPAY_PLAN_ID in Railway env points to the new ₹499/month plan
- Railway is redeployed and active
</success_criteria>

<output>
After completion, create `.planning/phases/10-digital-health-card/10-01-SUMMARY.md`
</output>
