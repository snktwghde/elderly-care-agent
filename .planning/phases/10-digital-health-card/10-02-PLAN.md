---
phase: 10-digital-health-card
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/services/claude.js
  - src/services/health-card.js
  - src/webhook/whatsapp.js
autonomous: true
requirements:
  - REQ-10-02
  - REQ-10-03
  - REQ-10-04
  - REQ-10-05
  - REQ-10-09

must_haves:
  truths:
    - "Claude intent parser recognises 'health card', 'medical card', 'show health card', 'set up health card', 'update blood group' etc."
    - "generateHealthCard() produces formatted WhatsApp text with no Claude API call"
    - "After onboarding completes, user receives a health card setup offer with the exact prompt text from CONTEXT.md"
    - "Partial health card setup is allowed — any step can be skipped with 'skip'"
  artifacts:
    - path: "src/services/claude.js"
      provides: "Extended SYSTEM_PROMPT with health card intents"
      contains: "setup_health_card | show_health_card | update_health_card"
    - path: "src/services/health-card.js"
      provides: "Health card generation + setup state machine"
      exports:
        - generateHealthCard
        - sendHealthCardOffer
        - startHealthCardSetup
        - handleHealthCardSetup
    - path: "src/webhook/whatsapp.js"
      provides: "Health card offer sent after onboarding + offer_pending state handler"
      contains: "health_card_offer_pending"
  key_links:
    - from: "src/webhook/whatsapp.js sendTrialStartedMessage"
      to: "src/services/health-card.js sendHealthCardOffer"
      via: "called after onboarding completes"
      pattern: "sendHealthCardOffer"
    - from: "src/services/health-card.js generateHealthCard"
      to: "recipient.medication_schedule"
      via: "reuses existing medication data — no new storage"
      pattern: "medication_schedule"
---

<objective>
Extend the Claude intent parser to recognise health card intents. Create the health-card.js service with the template generator and setup flow state machine. Wire the health card offer into the post-onboarding flow.

Purpose: This plan delivers the core health card infrastructure. No Claude API call for card generation — pure string template fill from Supabase data.
Output: Extended claude.js, new health-card.js, onboarding offer wired in whatsapp.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-digital-health-card/10-CONTEXT.md
@src/services/claude.js
@src/services/onboarding.js
@src/webhook/whatsapp.js
@.planning/phases/10-digital-health-card/10-01-SUMMARY.md

<interfaces>
<!-- From src/services/supabase.js — functions health-card.js will import -->
```javascript
export async function updateAccount(accountPhone, fields) // updates accounts row
export async function updateCareRecipient(accountPhone, fields) // updates care_recipients row
```

<!-- From src/services/whatsapp.js — sendTextMessage signature -->
```javascript
export async function sendTextMessage(to, body) // sends WhatsApp message
```

<!-- accounts table shape (relevant fields for pending_action pattern) -->
// account.pending_action: string | null  — current state in a multi-step flow
// account.account_phone: string          — E.164 phone number
// account.account_type: 'self'|'caregiver'

<!-- care_recipients table shape (relevant fields) -->
// recipient.recipient_name: string
// recipient.preferred_language: 'hindi'|'marathi'|'english'
// recipient.family_contacts: string[]
// recipient.medication_schedule: [{name, frequency, times}]  — REUSE for health card medications
// recipient.blood_group: string | null     — NEW (from Plan 01 migration)
// recipient.allergies: [{type, name}]     — NEW
// recipient.major_illnesses: string[]     — NEW
// recipient.surgeries: [{name, date}]     — NEW
// recipient.medical_history: string | null — NEW
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SYSTEM_PROMPT in claude.js with health card intents</name>
  <files>src/services/claude.js</files>
  <read_first>
    - src/services/claude.js (full file — read the SYSTEM_PROMPT constant at lines 7-51 before editing)
  </read_first>
  <action>
Make two changes to `src/services/claude.js`. Do not touch parseIntent, parseAppointmentDetails, classifyClinicSpeech, or any function signatures.

CHANGE 1 — The intent enum line (around line 10) currently reads:
```
"intent": "<one of: book_appointment | confirm_appointment | medication_reminder | sos | status_check | unknown>",
```
Replace with:
```
"intent": "<one of: book_appointment | confirm_appointment | medication_reminder | sos | status_check | setup_health_card | show_health_card | update_health_card | unknown>",
```

CHANGE 2 — The details object currently ends after medication_name. Add two new fields:
```json
{
  "details": {
    "appointment_type": "...",
    "specialty": "...",
    "doctor_name": "...",
    "date_hint": "...",
    "medication_name": "...",
    "health_card_field": "<blood_group|allergies|major_illnesses|surgeries|medical_history|null — which field the user wants to update>",
    "health_card_value": "<the new value if user stated it, else null>"
  }
}
```

CHANGE 3 — Add example phrases AFTER the existing "Status:" line (around line 43), before the closing backtick of SYSTEM_PROMPT:

```
Health card — show (user wants to see their health card):
- English: "health card", "medical card", "show health card", "show my health card", "medical summary", "my medical info"
- Marathi: "health card dakhav", "maza health card", "health card pahije", "medical card"
- Hindi: "health card dikhao", "medical card bhejo", "mera health card dikhao", "medical summary chahiye"

Health card — setup (user wants to create/set up their health card):
- "set up health card", "create health card", "setup health card", "health card banao"
- "health card setup karo", "health card bana"

Health card — update (user wants to update a specific field):
- "update blood group", "change blood group", "add allergy", "new allergy", "add surgery", "surgery add karo"
- "update medical history", "add illness", "blood group update karo", "allergy add karo"
- Set health_card_field to: blood_group | allergies | major_illnesses | surgeries | medical_history
- Set health_card_value to the stated value if mentioned (e.g., "update blood group to B+" → health_card_field: "blood_group", health_card_value: "B+")
```

ponytail: Only edit the SYSTEM_PROMPT string. All other code (parseIntent, model selection, token limits) stays unchanged.
  </action>
  <verify>
    <automated>grep -c "setup_health_card\|show_health_card\|update_health_card" src/services/claude.js</automated>
  </verify>
  <done>
    grep returns 3 or more (intent enum line + example phrases). SYSTEM_PROMPT still contains all original intents. health_card_field and health_card_value appear in the details object.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/services/health-card.js with generator + setup flow</name>
  <files>src/services/health-card.js</files>
  <read_first>
    - src/services/onboarding.js (full file — pattern to follow for multi-step state machine using updateAccount)
    - .planning/phases/10-digital-health-card/10-CONTEXT.md (health card format section — the exact template with emoji and separator lines)
  </read_first>
  <action>
Create `src/services/health-card.js`. This file MUST NOT call the Claude API. Zero Anthropic SDK imports.

Import only:
```javascript
import { updateAccount, updateCareRecipient } from './supabase.js';
import { sendTextMessage } from './whatsapp.js';
```

Export these four functions:

---

**1. generateHealthCard(recipient)**

Pure string interpolation — no async, no external calls.

```javascript
export function generateHealthCard(recipient) {
  const name = recipient?.recipient_name || 'Patient';
  const bloodGroup = recipient?.blood_group || 'Not provided';
  const medications = (recipient?.medication_schedule || []).map(m => m.name).join(', ') || 'Not provided';
  const allergies = (recipient?.allergies || []).map(a => a.name).join(', ') || 'Not provided';
  const illnesses = (recipient?.major_illnesses || []).join(', ') || 'Not provided';
  const surgeries = (recipient?.surgeries || [])
    .map(s => s.date ? `${s.name} (${s.date})` : s.name).join(', ') || 'Not provided';
  const history = recipient?.medical_history || 'Not provided';
  const emergency = (recipient?.family_contacts || [])[0] || 'Not provided';

  return [
    `🩺 *Health Card — ${name}*`,
    `─────────────────────`,
    `🩸 Blood Group: ${bloodGroup}`,
    `💊 Current Medications: ${medications}`,
    `⚠️ Allergies: ${allergies}`,
    `🏥 Major Illnesses: ${illnesses}`,
    `🔪 Surgeries: ${surgeries}`,
    `📋 Medical History: ${history}`,
    `📞 Emergency Contact: ${emergency}`,
    `─────────────────────`,
  ].join('\n');
}
```

---

**2. sendHealthCardOffer(phone, lang)**

Sends the offer message via WhatsApp and sets pending_action = 'health_card_offer_pending'.
Uses the EXACT English prompt text from CONTEXT.md (REQ-10-09):

```javascript
export async function sendHealthCardOffer(phone, lang) {
  const msg = {
    english: `Doctors often ask for current medications and medical history during visits — this lets you share it instantly.\n\nWould you like to set up your health card? (Takes about 5 minutes)\n\nReply *Yes* to set up or *No* to skip for now.`,
    marathi: `डॉक्टरांना visit दरम्यान नेहमी medications आणि medical history विचारावी लागते — health card असल्यास ते लगेच share करता येते.\n\nतुमचे health card सेट करायचे आहे का? (सुमारे 5 मिनिटे)\n\n*हो* म्हणा सेट करण्यासाठी किंवा *नको* म्हणा नंतरसाठी.`,
    hindi: `Doctors visit के दौरान हमेशा medications और medical history पूछते हैं — health card होने से वो तुरंत share होती है.\n\nक्या आप अपना health card सेट करना चाहते हैं? (लगभग 5 मिनट)\n\n*हाँ* कहें सेट करने के लिए या *नहीं* बाद के लिए.`,
  }[lang] || `Doctors often ask for current medications and medical history during visits — this lets you share it instantly.\n\nWould you like to set up your health card? (Takes about 5 minutes)\n\nReply *Yes* to set up or *No* to skip for now.`;

  await updateAccount(phone, { pending_action: 'health_card_offer_pending' });
  await sendTextMessage(phone, msg);
}
```

---

**3. startHealthCardSetup(accountPhone, lang)**

Sets pending_action to begin the setup flow and returns the first question.

```javascript
export async function startHealthCardSetup(accountPhone, lang) {
  await updateAccount(accountPhone, { pending_action: 'health_card_blood_group' });
  return {
    english: `Let's set up your health card.\n\nWhat is your *blood group*? (e.g., B+, O-, AB+)\n\nType *skip* if you don't know or want to fill it later.`,
    marathi: `तुमचे health card सेट करूया.\n\nतुमचा *blood group* काय आहे? (उदा. B+, O-, AB+)\n\nमाहीत नसल्यास किंवा नंतर भरायचे असल्यास *skip* टाइप करा.`,
    hindi: `आपका health card सेट करते हैं.\n\nआपका *blood group* क्या है? (जैसे B+, O-, AB+)\n\nनहीं पता या बाद में भरना हो तो *skip* लिखें.`,
  }[lang];
}
```

---

**4. handleHealthCardSetup(account, messageText, lang, recipient)**

State machine for collecting health card fields. Each step saves the field immediately to care_recipients and moves to the next step. Accepts 'skip' at every step.

```javascript
const VALID_BLOOD_GROUPS = new Set(['A+', 'A-', 'B+', 'B-', 'AB+', 'AB-', 'O+', 'O-', 'A1+', 'A1-', 'A1B+', 'A1B-']);
const SKIP_RE = /^(skip|don'?t know|nahi pata|pata nahi|माहीत नाही|पता नहीं|नाही|नको|नहीं)$/i;

export async function handleHealthCardSetup(account, messageText, lang, recipient) {
  const { pending_action, account_phone } = account;
  const input = messageText.trim().slice(0, 500);
  const isSkip = SKIP_RE.test(input);

  // ── health_card_offer_pending ──────────────────────────────────────────────
  if (pending_action === 'health_card_offer_pending') {
    const isYes = /^(yes|हो|ho|haan|हाँ|ha|हा|ok|okay|हो\s*जी|हां|ho ja)$/i.test(input);
    const isNo  = /^(no|nahi|नाही|नहीं|nko|nako|नको|skip|later|baad mein)$/i.test(input);

    if (isYes) return await startHealthCardSetup(account_phone, lang);

    // No or unrecognised → clear state, acknowledge
    await updateAccount(account_phone, { pending_action: null });
    return {
      english: `No problem. You can set it up later — just type *health card* anytime.`,
      marathi: `ठीक आहे. नंतर केव्हाही *health card* टाइप करा.`,
      hindi: `कोई बात नहीं। बाद में कभी भी *health card* लिखें.`,
    }[lang];
  }

  // ── health_card_blood_group ────────────────────────────────────────────────
  if (pending_action === 'health_card_blood_group') {
    if (!isSkip) {
      const normalised = input.toUpperCase().replace(/\s+/g, '');
      if (!VALID_BLOOD_GROUPS.has(normalised)) {
        return {
          english: `Please enter a valid blood group (e.g., A+, B-, O+, AB+) or type *skip* to leave it blank.`,
          marathi: `कृपया valid blood group टाका (उदा. A+, B-, O+, AB+) किंवा रिकामे सोडायचे असल्यास *skip* टाइप करा.`,
          hindi: `कृपया valid blood group डालें (जैसे A+, B-, O+, AB+) या *skip* लिखें अगर खाली छोड़ना है.`,
        }[lang];
      }
      await updateCareRecipient(account_phone, { blood_group: normalised });
    }
    await updateAccount(account_phone, { pending_action: 'health_card_allergies' });
    return {
      english: `Got it!\n\nDo you have any *allergies*? List them separated by commas (e.g., Penicillin, Peanuts, Dust).\n\nType *skip* if none.`,
      marathi: `ठीक आहे!\n\nतुम्हाला कोणती *allergies* आहेत का? स्वल्पविरामाने विभागून लिहा (उदा. Penicillin, Peanuts).\n\nनसल्यास *skip* टाइप करा.`,
      hindi: `ठीक है!\n\nक्या आपको कोई *allergies* हैं? उन्हें comma से अलग करके लिखें (जैसे Penicillin, Peanuts).\n\n*skip* लिखें अगर कोई नहीं.`,
    }[lang];
  }

  // ── health_card_allergies ─────────────────────────────────────────────────
  if (pending_action === 'health_card_allergies') {
    if (!isSkip) {
      const items = input.split(/,|\n/).map(s => s.trim()).filter(Boolean).slice(0, 10);
      const allergies = items.map(name => ({ type: 'other', name: name.slice(0, 100) }));
      await updateCareRecipient(account_phone, { allergies });
    }
    await updateAccount(account_phone, { pending_action: 'health_card_illnesses' });
    return {
      english: `Got it!\n\nDo you have any *major illnesses*? (e.g., Diabetes, High blood pressure, Asthma)\n\nType *skip* if none.`,
      marathi: `ठीक आहे!\n\nतुम्हाला कोणते *प्रमुख आजार* आहेत का? (उदा. Diabetes, High blood pressure, Asthma)\n\nनसल्यास *skip* टाइप करा.`,
      hindi: `ठीक है!\n\nक्या आपको कोई *बड़ी बीमारियाँ* हैं? (जैसे Diabetes, High blood pressure, Asthma)\n\n*skip* लिखें अगर कोई नहीं.`,
    }[lang];
  }

  // ── health_card_illnesses ─────────────────────────────────────────────────
  if (pending_action === 'health_card_illnesses') {
    if (!isSkip) {
      const illnesses = input.split(/,|\n/).map(s => s.trim()).filter(Boolean).slice(0, 10);
      await updateCareRecipient(account_phone, { major_illnesses: illnesses });
    }
    await updateAccount(account_phone, { pending_action: 'health_card_surgeries' });
    return {
      english: `Got it!\n\nHave you had any *surgeries*? List them with year if known (e.g., Appendix removal 2019, Cataract surgery 2022).\n\nType *skip* if none.`,
      marathi: `ठीक आहे!\n\nतुम्हाला कोणत्या *शस्त्रक्रिया* झाल्या आहेत का? शक्य असल्यास वर्षासह लिहा (उदा. Appendix removal 2019).\n\nनसल्यास *skip* टाइप करा.`,
      hindi: `ठीक है!\n\nआपकी कोई *surgeries* हुई हैं? हो सके तो साल के साथ लिखें (जैसे Appendix removal 2019).\n\n*skip* लिखें अगर कोई नहीं.`,
    }[lang];
  }

  // ── health_card_surgeries ─────────────────────────────────────────────────
  if (pending_action === 'health_card_surgeries') {
    if (!isSkip) {
      const items = input.split(/,|\n/).map(s => s.trim()).filter(Boolean).slice(0, 10);
      const surgeries = items.map(item => {
        const yearMatch = item.match(/\b(19|20)\d{2}\b/);
        const date = yearMatch ? yearMatch[0] : null;
        const name = item.replace(/\b(19|20)\d{2}\b/, '').trim().replace(/\s+/g, ' ') || item;
        return { name: name.slice(0, 150), date };
      });
      await updateCareRecipient(account_phone, { surgeries });
    }
    await updateAccount(account_phone, { pending_action: 'health_card_history' });
    return {
      english: `Almost done!\n\nIs there any other *medical history* to add? (e.g., family history of heart disease, past hospitalisations)\n\nType *skip* to finish.`,
      marathi: `जवळजवळ झाले!\n\nइतर *वैद्यकीय इतिहास* काही सांगायचे आहे का? (उदा. कुटुंबात हृदयरोगाचा इतिहास)\n\nसंपवायचे असल्यास *skip* टाइप करा.`,
      hindi: `लगभग हो गया!\n\nकोई और *medical history* बताना है? (जैसे परिवार में दिल की बीमारी, पुराने hospitalisations)\n\nखत्म करने के लिए *skip* लिखें.`,
    }[lang];
  }

  // ── health_card_history ───────────────────────────────────────────────────
  if (pending_action === 'health_card_history') {
    if (!isSkip) {
      await updateCareRecipient(account_phone, { medical_history: input.slice(0, 500) });
    }
    await updateAccount(account_phone, { pending_action: null });
    return {
      english: `✅ Health card saved!\n\nType *health card* anytime to see it. During an SOS, it will be sent automatically to your family.`,
      marathi: `✅ Health card जतन झाले!\n\nकेव्हाही *health card* टाइप करा पाहण्यासाठी. SOS वेळी ते आपोआप कुटुंबाला पाठवले जाईल.`,
      hindi: `✅ Health card सेव हो गया!\n\nकभी भी *health card* लिखें देखने के लिए। SOS के समय यह automatically परिवार को भेजा जाएगा.`,
    }[lang];
  }

  // Unknown setup state — reset
  await updateAccount(account_phone, { pending_action: null });
  return {
    english: `Something went wrong. Type *health card* to start over.`,
    marathi: `काहीतरी चुकले. पुन्हा सुरू करण्यासाठी *health card* टाइप करा.`,
    hindi: `कुछ गलत हुआ। फिर से शुरू करने के लिए *health card* लिखें.`,
  }[lang];
}
```

Export all four: generateHealthCard, sendHealthCardOffer, startHealthCardSetup, handleHealthCardSetup.

Security notes in the file:
- Input truncated to 500 chars before any processing
- Field values truncated individually (100 chars per allergy name, 150 chars per surgery name, 500 chars for medical_history)
- Blood group validated against allowlist before storage
- No health card data is passed to Claude API
  </action>
  <verify>
    <automated>node -e "import('./src/services/health-card.js').then(m => { console.log(typeof m.generateHealthCard, typeof m.sendHealthCardOffer, typeof m.startHealthCardSetup, typeof m.handleHealthCardSetup); }).catch(e => console.error(e.message))"</automated>
  </verify>
  <done>
    node import test prints: "function function function function" (all four exports are functions). generateHealthCard called with a mock recipient object returns a string starting with "🩺 *Health Card". No Anthropic SDK imported anywhere in the file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire health card offer after onboarding completes in whatsapp.js</name>
  <files>src/webhook/whatsapp.js</files>
  <read_first>
    - src/webhook/whatsapp.js (lines 1-15 — import block; lines 83-100 — onboarding block where sendTrialStartedMessage is called; lines 996-1018 — sendTrialStartedMessage function)
  </read_first>
  <action>
Make two targeted changes to `src/webhook/whatsapp.js`.

CHANGE 1 — Add import at the top of the file with the other service imports (around lines 1-9):

```javascript
import { sendHealthCardOffer, handleHealthCardSetup } from '../services/health-card.js';
```

Add this import alongside the existing imports from services (after the razorpay.js import line).

CHANGE 2 — In the onboarding completion block (around lines 92-97), find this exact code:

```javascript
const updated = await getOrCreateAccount(senderPhone);
if (updated.onboarding_complete) {
  await sendTrialStartedMessage(senderPhone);
}
```

Replace with:

```javascript
const updated = await getOrCreateAccount(senderPhone);
if (updated.onboarding_complete) {
  await sendTrialStartedMessage(senderPhone);
  const newRecipient = await getPrimaryCareRecipient(senderPhone);
  const offerLang = newRecipient?.preferred_language || 'english';
  await sendHealthCardOffer(senderPhone, offerLang).catch(e =>
    console.error('[Health card offer failed]', e.message)
  );
}
```

Note: getPrimaryCareRecipient is already imported at line 4 of whatsapp.js. No new import needed for it.

No other changes to whatsapp.js in this task. The handleHealthCardSetup import will be wired into handlePendingAction in Plan 03.

ponytail: sendHealthCardOffer sends the WhatsApp message AND sets pending_action='health_card_offer_pending' internally. whatsapp.js just calls it.
  </action>
  <verify>
    <automated>grep -n "sendHealthCardOffer\|health_card_offer_pending\|health-card" src/webhook/whatsapp.js</automated>
  </verify>
  <done>
    grep shows: import line references '../services/health-card.js', sendHealthCardOffer appears in the onboarding completion block. No syntax errors — node --check src/webhook/whatsapp.js exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user WhatsApp → health card setup flow | User input enters blood group, allergy, illness, surgery, history fields |
| health-card.js → Supabase care_recipients | PHI data written to DB via service role key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Information Disclosure | health card data logged via logMessage outgoing_reply | mitigate | Plan 03 redacts health card content from logMessage (replace with '[health card delivered]'). This plan does not generate log entries for health card content. |
| T-10-02-02 | Denial of Service | large user input to allergies/illnesses/surgeries steps | mitigate | Input truncated to 500 chars before processing; individual field values truncated (100/150/500 chars). Max 10 items per array field. |
| T-10-02-03 | Tampering | invalid blood group accepted and stored | mitigate | Blood group validated against VALID_BLOOD_GROUPS Set (allowlist) before updateCareRecipient call. Invalid input → re-ask, not stored. |
| T-10-02-04 | Information Disclosure | Claude API receives health card phrases for intent parsing | accept | Claude only sees the raw user message text for intent classification (e.g., "update blood group to B+"). No stored health data is passed to Claude. |
</threat_model>

<verification>
1. `grep -c "setup_health_card\|show_health_card\|update_health_card" src/services/claude.js` — returns >= 3
2. `node -e "import('./src/services/health-card.js').then(m => console.log(Object.keys(m)))"` — prints array including generateHealthCard, sendHealthCardOffer, startHealthCardSetup, handleHealthCardSetup
3. `grep "sendHealthCardOffer" src/webhook/whatsapp.js` — returns the import line and the onboarding block call
4. `grep "health-card" src/webhook/whatsapp.js` — shows the import from ../services/health-card.js
5. `grep "Anthropic\|anthropic" src/services/health-card.js` — returns nothing (zero API calls)
</verification>

<success_criteria>
- Claude intent parser recognises setup_health_card, show_health_card, update_health_card intents
- generateHealthCard(recipient) returns formatted WhatsApp text using medication_schedule, family_contacts, and new health card columns — no Claude API call
- After onboarding completes, user receives health card setup offer with the CONTEXT.md prompt text
- Setup flow collects all 5 fields conversationally, allows 'skip' at each step, saves each field immediately to care_recipients
- Blood group validated against allowlist before storage
- All user inputs truncated before storage
</success_criteria>

<output>
After completion, create `.planning/phases/10-digital-health-card/10-02-SUMMARY.md`
</output>
